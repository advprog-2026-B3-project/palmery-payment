# palmery-payment

## Local Run

The payment backend runs on http://localhost:8082 by default.
You can override it with `SERVER_PORT`, but local development and docker compose are both configured for 8082.
